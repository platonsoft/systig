import { BasicAuttenticacionInterceptor } from './basic-auttenticacion-interceptor';

describe('BasicAuttenticacionInterceptor', () => {
  it('should create an instance', () => {
    expect(new BasicAuttenticacionInterceptor()).toBeTruthy();
  });
});
