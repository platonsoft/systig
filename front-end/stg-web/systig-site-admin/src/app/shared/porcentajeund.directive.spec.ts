import { PorcentajeundDirective } from './porcentajeund.directive';

describe('PorcentajeundDirective', () => {
  it('should create an instance', () => {
    const directive = new PorcentajeundDirective();
    expect(directive).toBeTruthy();
  });
});
