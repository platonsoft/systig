import { PesocolDirective } from './pesocol.directive';

describe('PesocolDirective', () => {
  it('should create an instance', () => {
    const directive = new PesocolDirective();
    expect(directive).toBeTruthy();
  });
});
