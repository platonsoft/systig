import { BasicHttpInterceptor } from './basic-http-interceptor';

describe('BasicHttpInterceptor', () => {
  it('should create an instance', () => {
    expect(new BasicHttpInterceptor()).toBeTruthy();
  });
});
